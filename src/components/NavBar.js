import React from "react";
export default function NavBar(){
    return(
        <nav className="Nav-bar">
            <ul>
                <a href="#">Home </a>
                <a href="#">About </a>
                <a href="#">Contact </a>

            </ul>
        </nav>
    );
}